name: stm_extensions
version: 0.1.5

environment:
  sdk: ">=3.11.5"
  flutter: '>=3.41.9'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0
flutter: